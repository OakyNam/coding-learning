# 01 - Setup and Install

Install the .NET SDK from https://dotnet.microsoft.com/download.

Check the install:

```bash
dotnet --version
```

Create a console app:

```bash
dotnet new console -n HelloCSharp
cd HelloCSharp
dotnet run
```

Practice: edit `Program.cs` so it prints your own message.
