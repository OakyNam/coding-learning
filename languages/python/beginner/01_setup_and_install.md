# 01 - Setup and Install

Install Python from https://www.python.org/downloads/ and make sure `python` or `py` works in your terminal.

```bash
python --version
```

Create a first file named `hello.py`:

```python
print("Hello, Python!")
```

Run it:

```bash
python hello.py
```

Practice: change the message and run the file again.
