# 09 - Functions

Functions name reusable code.

```c
int add(int a, int b) {
    return a + b;
}

int result = add(2, 3);
```

Declare functions before using them, or place prototypes near the top.

```c
int add(int a, int b);
```

Practice: write a function that returns the square of an integer.
